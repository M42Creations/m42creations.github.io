# M42 Creations
## Local Testing
To do basic local testing, run <code>python3 -m http.server</code> from the root directory of the project (where <code>index.html</code> exists) and navigate to http://localhost:8000